#include <stdio.h>

/**
 * main - produce an output according to a format
 *
 * Return: always 0
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
